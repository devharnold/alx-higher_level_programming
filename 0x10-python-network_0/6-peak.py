#!/usr/bin/python3

"""finding peak number in list of integers"""
def find_peak(list_of_integers):
    list_of_integers = [5, 6, 7, 9, 8]
    for num in list_of_integers:
        if num >= :
            result = (find_peak(list_of_integers=[]))
            print (result)


if __name__ == "__main__":
    list_of_numbers = [1, 4, 5, 9, 6, 7]
    print(find_peak(list_of_integers=[1, 4, 5, 9, 6, 7]))
